{
  "name": "server",
  "version": "0.1.0",
  "main": "__generated__/AppEntry.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "dev": "ts-node-dev --inspect --clear --transpile-only --no-notify -r tsconfig-paths/register src",
    "start": "node dist",
    "lint": "eslint src --ext=.ts --fix",
    "precommit": "lint-staged",
    "test": "echo >> /dev/null",
    "typeorm": "ts-node-dev node_modules/.bin/typeorm"
  },
  "dependencies": {
    "date-fns": "^2.17.0",
    "express": "^4.17.1",
    "pg": "^8.5.1",
    "typeorm": "^0.2.31",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.11",
    "ts-node-dev": "^1.1.1"
  }
}
